# TEMPLATE ONLY -- this file IS committed to git.
# Copy it to `.env` and put real values there; `.env` is gitignored.
#
#     cp .env.example .env
#
# Never put a real key in THIS file.

# Each person uses their own. Get yours from https://wandb.ai/authorize
WANDB_API_KEY=

# Our shared W&B team. Not a secret -- safe to keep here.
WANDB_ENTITY=mahdi-sadman-abrar
